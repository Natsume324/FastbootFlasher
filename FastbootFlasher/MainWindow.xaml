<Window x:Class="FastbootFlasher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FastbootFlasher"
        mc:Ignorable="d"
        Title="{DynamicResource WindowTitle}" Height="610" Width="1024">
    <TabControl FontSize="16">
        <TabItem Header="{DynamicResource Main}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="18*" />
                        
                        <RowDefinition Height="2*" />
                        <RowDefinition Height="1.8*" />
                        <RowDefinition Height="*" />
                       
                    </Grid.RowDefinitions>

   <DataGrid x:Name="PartitionDataGrid"
          Grid.Row="0"
          ItemsSource="{Binding Partitions}"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          CanUserDeleteRows="False"
          CanUserSortColumns="False"
          HeadersVisibility="Column"
          GridLinesVisibility="None"
          RowBackground="Transparent"
          AlternatingRowBackground="#F8F8F8"
          SelectionUnit="FullRow">
                        <DataGrid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="{DynamicResource ExtractPart}" Click="ExtractSelectedPart_Click"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="56" Binding="{Binding Index}" IsReadOnly="True" CanUserSort="False">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="Content" Value="{DynamicResource Num}" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Width="180" Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" CanUserSort="False">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="Content" Value="{DynamicResource Part}" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Width="120" Binding="{Binding Size}" IsReadOnly="True" CanUserSort="False">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="Content" Value="{DynamicResource Size}" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Width="275" Binding="{Binding SourceFile}" IsReadOnly="True" CanUserSort="False">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="Content" Value="{DynamicResource Source}" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Grid Grid.Row="1" Margin="0,5,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button x:Name="Flash_Btn" Grid.Column="0" Content="{DynamicResource Flash}" Margin="0,0,5,0" Click="Flash_Btn_Click" />
                        <Button Grid.Column="1" Content="{DynamicResource Reboot}" Margin="5,0,0,0" />
                    </Grid>
                    <Grid Grid.Row="2" Margin="0,5,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="10*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="FilePath_Box" Grid.Column="0" IsReadOnly="True"/>
                        <Button x:Name="Load_Btn" Grid.Column="1" Content="{DynamicResource Load}" Margin="5,0,0,0" Click="Load_Btn_Click" />
                    </Grid>
                    <!-- 进度条 -->
                    <ProgressBar x:Name="progressBar" Grid.Row="3" Height="20" Minimum="0" Maximum="100" Margin="0,0,0,0"/>
                    <TextBlock x:Name="progressText" Grid.Row="3" Height="20" Text="0%" HorizontalAlignment="Center"/>

                </Grid>
                <GroupBox Grid.Column="1" Header="{DynamicResource Log}">
                    <TextBox x:Name="Log_Box" IsReadOnly="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"/>

                </GroupBox>
            </Grid>
        </TabItem>
        <TabItem Header="{DynamicResource Func}">

        </TabItem>
        <TabItem Header="{DynamicResource Settings}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Margin="0,5" Grid.Column="0">
                    <Label Content="{DynamicResource Lang}" Width="Auto" VerticalAlignment="Center"/>
                    <ComboBox x:Name="LangComboBox" Width="Auto" SelectionChanged="LangComboBox_SelectionChanged">
                        <ComboBoxItem Content="简体中文" Tag="zh-cn" IsSelected="True"/>
                        <ComboBoxItem Content="English" Tag="en-us"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </TabItem>
        <TabItem Header="{DynamicResource About}">
        </TabItem>
    </TabControl>
</Window>
